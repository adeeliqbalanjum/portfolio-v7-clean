const navToggle=document.querySelector('[data-nav-toggle]');const navMenu=document.querySelector('[data-nav-menu]');if(navToggle&&navMenu){navToggle.addEventListener('click',()=>{const open=navMenu.classList.toggle('is-open');navToggle.setAttribute('aria-expanded',String(open));});navMenu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{navMenu.classList.remove('is-open');navToggle.setAttribute('aria-expanded','false');}));}
const revealItems=document.querySelectorAll('[data-reveal]');if('IntersectionObserver'in window){const io=new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target);}})},{threshold:.16});revealItems.forEach(el=>io.observe(el));}else{revealItems.forEach(el=>el.classList.add('is-visible'));}
const form=document.querySelector('[data-contact-form]');if(form){form.addEventListener('submit',(e)=>{e.preventDefault();const email=form.getAttribute('data-email')||'adeeliqbalajum@gmail.com';const name=encodeURIComponent(form.querySelector('[name="name"]')?.value||'');const from=encodeURIComponent(form.querySelector('[name="email"]')?.value||'');const message=encodeURIComponent(form.querySelector('[name="message"]')?.value||'');window.location.href=`mailto:${email}?subject=Portfolio inquiry from ${name}&body=Name:%20${name}%0AEmail:%20${from}%0A%0A${message}`;});}
